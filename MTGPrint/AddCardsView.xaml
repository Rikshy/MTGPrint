<Window x:Class="MTGPrint.AddCardsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MTGPrint.ViewModels"
        mc:Ignorable="d"
        Title="AddCards" Height="450" Width="800">
    <Window.DataContext>
        <local:AddCardsViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="25"/>
        </Grid.RowDefinitions>
        <TextBox Grid.Row="0" Text="{Binding ImportCards}"  TextWrapping="Wrap" AcceptsReturn="True" />
        <Button Content="Import" Grid.Row="1" IsDefault="True" Click="Button_Click" />
    </Grid>
</Window>
